export const Contact = (props) => {
    // export const Contact = ({item}) => {


  return (
    <>
        <li>
            {props.country}
            {props.name}

            {/* {item?.country}
            {item?.name} */}
        </li>
    </>
  )
}
